language: node_js

node_js:
  - "stable"

services:
  - docker

before_deploy: 
  - docker build -t tradeforgoals.azurecr.io/tradeforgoals/frontend .

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master
